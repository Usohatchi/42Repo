<!DOCTYPE html>

<html>
    
<head>
	<meta charset="utf-8">
	<title></title>
	<style></style>
    <link href="main.css" rel="stylesheet" type="text/css">
</head>
    
<body>
	<div id="container">
        <ul class="nav-bar">
            <li style="float: left; border: none" class="nav-bar-links"><%= link_to 'Header Text', projects_path, {:class => "nav-link"} %></li>
            <li class="nav-bar-links"><%= link_to image_tag("http://www.iconsdb.com/icons/preview/white/plus-xxl.png",  :class => "nav-link"), new_project_path %></li>
        </ul>
		<div id="logo-holder">
			<img id="logo" src="http://www.morrell-middleton.co.uk/wp-content/uploads/logo-placeholder.jpg" alt="placeholder">
		</div>
        <div class="image-list-holder list-left">
            <ul class="image-list">
                <% (0..@projects.length).step(4) do |index| %>
                    <li class="display-img-holder">
                        <% if !(@projects[index].nil?) %>
                             <%= link_to image_tag(@projects[index].image_link,  :class => "display-img"), project_path(@projects[index]) %>
                             <%= link_to project_path(@projects[index]) do %>
                                <figcaption><p><%= @projects[index].title %></p></figcaption>
                             <% end %>
                        <% end %>
                    </li>
                <% end %>
            </ul>
        </div>
        <div class="image-list-holder list-left">
            <ul class="image-list">
                <% (1..@projects.length).step(4) do |index| %>
                    <li class="display-img-holder">
                        <% if !(@projects[index].nil?) %>
                             <%= link_to image_tag(@projects[index].image_link,  :class => "display-img"), project_path(@projects[index]) %>
                             <%= link_to project_path(@projects[index]) do %>
                                <figcaption><p><%= @projects[index].title %></p></figcaption>
                             <% end %>
                        <% end %>
                    </li>
                <% end %>
            </ul>
        </div>
        <div class="image-list-holder list-right">
            <ul class="image-list">
                <% (3..@projects.length).step(4) do |index| %>
                    <li class="display-img-holder">
                        <% if !(@projects[index].nil?) %>
                             <%= link_to image_tag(@projects[index].image_link,  :class => "display-img"), project_path(@projects[index]) %>
                             <%= link_to project_path(@projects[index]) do %>
                                <figcaption><p><%= @projects[index].title %></p></figcaption>
                             <% end %>
                        <% end %>
                    </li>
                <% end %>
            </ul>
        </div>
        <div class="image-list-holder list-right">
            <ul class="image-list">
                <% (2..@projects.length).step(4) do |index| %>
                    <li class="display-img-holder">
                        <% if !(@projects[index].nil?) %>
                             <%= link_to image_tag(@projects[index].image_link,  :class => "display-img"), project_path(@projects[index]) %>
                             <%= link_to project_path(@projects[index]) do %>
                                <figcaption><p><%= @projects[index].title %></p></figcaption>
                             <% end %>
                        <% end %>
                    </li>
                <% end %>
            </ul>
        </div>
	</div>
</body>
    
</html>